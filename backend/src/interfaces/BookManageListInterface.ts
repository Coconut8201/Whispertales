export interface BookManageListInterface {
    bookName: string,               // 顯示的書本名字
    bookId: string,                 // 書本id storyId
    bookFirstImageBase64: string    // 封面頁的圖片base64 code
}